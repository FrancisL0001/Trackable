# Trackable frontend configuration. Copy to .env to override.
# Base URL of the backend API. In dev, Vite proxies /api to the backend, so the
# default empty value (same-origin) works out of the box.
VITE_API_BASE_URL=
